// import { get, post, put, del } from './request';

// const URL = '/api';
//const ROUTES_URL = `${URL}/routes`;

// export const getAll = () => get(ROUTES_URL);
// export const getById = id => get(`${ROUTES_URL}/${id}`);
// export const post = route => post(ROUTES_URL, route);
// export const put = route => put(`${ROUTES_URL}/${route._id}`, route);
// export const delete = id => del(`${ROUTES_URL}/${id}`, id);